import 'package:flutter/material.dart';

class Errorscreen extends StatelessWidget {
  static const String router = 'go';
  const Errorscreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const DefaultLayout
  }
}
